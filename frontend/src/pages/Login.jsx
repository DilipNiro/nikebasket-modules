// src/pages/Login.jsx — Page de connexion

// ================================================================
// TODO — Implémenter la page Login
// ================================================================
// Formulaire avec : email, password
// Appeler login(email, password) depuis AuthContext (module 10)
// Après connexion réussie → rediriger vers '/'
// Afficher les erreurs éventuelles
//
// Hooks : useState
// Navigation : useNavigate()

export default function Login() {
  return (
    <div style={{ maxWidth: 400, margin: '3rem auto', padding: '2rem' }}>
      <h2>Connexion</h2>
      <p>TODO : formulaire email + password, appel à l'API /auth/login</p>
    </div>
  );
}
