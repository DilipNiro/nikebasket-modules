# frontend/Dockerfile
# --------------------
# ================================================================
# TODO — Écrire le Dockerfile du frontend (même structure que backend)
# ================================================================
# Même pattern que le Dockerfile backend mais :
#   - EXPOSE 5173 (port Vite)
#   - CMD ["npm", "run", "dev", "--", "--host"]
#     → "--host" rend le serveur Vite accessible depuis l'extérieur du conteneur

# Écrivez votre Dockerfile ici :
